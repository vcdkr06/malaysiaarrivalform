const NoticeBanner = () => {
  return (
    <div className="w-full bg-accent">
      <div className="container mx-auto px-3 sm:px-6 py-1.5">
        <p className="text-center text-xs sm:text-sm font-semibold text-accent-foreground">
          Mandatory for all foreign travelers arriving in Malaysia
        </p>
      </div>
    </div>
  );
};

export default NoticeBanner;
